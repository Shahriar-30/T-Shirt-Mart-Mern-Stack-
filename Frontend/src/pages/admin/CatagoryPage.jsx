import React from 'react'
import { useSelector } from 'react-redux'

export const CatagoryPage = () => {
    let userToken = useSelector(state => console.log(sate))
  return (
    <div>CatagoryPage</div>
  )
}
